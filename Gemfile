# Manifest
#
# bundle install[ --without test development] pour les installer

source 'https://rubygems.org'

group :test do
  # Utiles pour les tests
  gem 'rspec', :require => 'spec'
  gem 'poltergeist'
end

group :development do
  # Utiles au développement
end

gem 'sassc'
gem 'mysql', ">= 0.5.2"
